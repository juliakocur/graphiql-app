export enum LocalStorageKeys {
  language = 'language',
}

export enum Languages {
  ru = 'ru',
  en = 'en',
}

export enum ValidationErrorsCodes {
  required = 'required',
  invalidEmail = 'invalidEmail',
  passwordLength = 'passwordLength',
  passwordUppercaseLetter = 'passwordUppercaseLetter',
  passwordLowercaseLetter = 'passwordLowercaseLetter',
  passwordSpecialChar = 'passwordSpecialChar',
  passwordDigit = 'passwordDigit',
}
